@model List<CookBook.Models.RecipeModel>
@{
    ViewData["Title"] = "Home Page";
}

@*
    @if (Model.Count() > 0)
{
    @foreach(var item in Model)
    {
        <div class="carousel-item">
            <img src="https://images.unsplash.com/photo-1546069901-ba9599a7e63c?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=580&q=80" alt="image of @item.Name">
            <div class="carousel-caption d-none d-md-block">
                <h5>@item.Name</h5>
                <p>@item.Description</p>
                <a class="btn btn-primary" asp-controller="Recipe" asp-action="GetRecipe" asp-route-id="@item.Id">View Recipe</a>
            </div>
        </div>
    }
    <div class="text-center">
    
    </div>
}
else {
    <a asp-controller="Recipe" asp-action="CreateRecipe" class="btn" style="margin-bottom: 15px; background-color:#fdc57b">
        <i class="bi bi-plus-circle"></i> &nbsp; Add New Recipe
    </a>
}*@